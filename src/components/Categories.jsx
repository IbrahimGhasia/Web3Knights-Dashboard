export default function Categories({ title }) {
	return (
		<div>
			<span className="py-2 px-3 md:px-5 my-2 rounded-full bg-gray-100 text-gray-400 text-xs md:text-md">
				{title}
			</span>
		</div>
	);
}
